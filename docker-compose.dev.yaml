version: "3.9"

services:
  nanoclaw-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: nanoclaw-dev
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
